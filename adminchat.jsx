// adminchat.jsx — the admin Channels view now lives in AdminChat (admin.jsx).
// Kept as a thin alias so any lingering reference to AdminChatScreen resolves safely.
Object.assign(window, { AdminChatScreen: window.AdminChat });
